interface:
  display_name: "OpenCode Handoff: Can Ask for Advice"
  short_description: "Let OpenCode do the task and ask for advice"
  default_prompt: "Use $handoff-opencode-with-advice to delegate this bounded task to OpenCode and let that worker ask for read-only advice."
